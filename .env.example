# Copy this file to `.env` and fill in your TMDB API key.
# Get one at https://www.themoviedb.org/settings/api
# The REACT_APP_ prefix is kept (via Vite envPrefix) for deployment compatibility.
REACT_APP_API_KEY=your_tmdb_api_key_here
